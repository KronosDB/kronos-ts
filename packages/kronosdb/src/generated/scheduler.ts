// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v3.19.1
// source: scheduler.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { TaggedEvent } from "./eventstore.js";

export const protobufPackage = "kronosdb.scheduler";

export type ScheduleAppendRequest = {
  /**
   * When the event should be appended, in milliseconds since epoch. A time
   * in the past fires on the next scheduler tick.
   */
  dueMs: bigint;
  /** The event to append, with the tags it should carry. */
  event:
    | TaggedEvent
    | undefined;
  /** Optional caller-supplied idempotency token. Generated when empty. */
  token: string;
}

export type ScheduleAppendResponse = {
  /** Identifies the schedule for cancellation. */
  token: string;
}

export type CancelScheduleRequest = {
  token: string;
}

export type CancelScheduleResponse = {
}

export type ListSchedulesRequest = {
}

/** A schedule that has neither fired nor been cancelled. */
export type Schedule = {
  token: string;
  dueMs: bigint;
  /** Type name of the event that will be appended. */
  eventName: string;
}

export type ListSchedulesResponse = {
  schedules: Schedule[];
}

function createBaseScheduleAppendRequest(): ScheduleAppendRequest {
  return { dueMs: 0n, event: undefined, token: "" };
}

export const ScheduleAppendRequest: MessageFns<ScheduleAppendRequest> = {
  encode(message: ScheduleAppendRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dueMs !== 0n) {
      if (BigInt.asIntN(64, message.dueMs) !== message.dueMs) {
        throw new globalThis.Error("value provided for field message.dueMs of type int64 too large");
      }
      writer.uint32(8).int64(message.dueMs);
    }
    if (message.event !== undefined) {
      TaggedEvent.encode(message.event, writer.uint32(18).fork()).join();
    }
    if (message.token !== "") {
      writer.uint32(26).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScheduleAppendRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScheduleAppendRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.dueMs = reader.int64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.event = TaggedEvent.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScheduleAppendRequest {
    return {
      dueMs: isSet(object.dueMs) ? BigInt(object.dueMs) : isSet(object.due_ms) ? BigInt(object.due_ms) : 0n,
      event: isSet(object.event) ? TaggedEvent.fromJSON(object.event) : undefined,
      token: isSet(object.token) ? globalThis.String(object.token) : "",
    };
  },

  toJSON(message: ScheduleAppendRequest): unknown {
    const obj: any = {};
    if (message.dueMs !== 0n) {
      obj.dueMs = message.dueMs.toString();
    }
    if (message.event !== undefined) {
      obj.event = TaggedEvent.toJSON(message.event);
    }
    if (message.token !== "") {
      obj.token = message.token;
    }
    return obj;
  },

  create(base?: DeepPartial<ScheduleAppendRequest>): ScheduleAppendRequest {
    return ScheduleAppendRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ScheduleAppendRequest>): ScheduleAppendRequest {
    const message = createBaseScheduleAppendRequest();
    message.dueMs = object.dueMs ?? 0n;
    message.event = (object.event !== undefined && object.event !== null)
      ? TaggedEvent.fromPartial(object.event)
      : undefined;
    message.token = object.token ?? "";
    return message;
  },
};

function createBaseScheduleAppendResponse(): ScheduleAppendResponse {
  return { token: "" };
}

export const ScheduleAppendResponse: MessageFns<ScheduleAppendResponse> = {
  encode(message: ScheduleAppendResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScheduleAppendResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScheduleAppendResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScheduleAppendResponse {
    return { token: isSet(object.token) ? globalThis.String(object.token) : "" };
  },

  toJSON(message: ScheduleAppendResponse): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    return obj;
  },

  create(base?: DeepPartial<ScheduleAppendResponse>): ScheduleAppendResponse {
    return ScheduleAppendResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ScheduleAppendResponse>): ScheduleAppendResponse {
    const message = createBaseScheduleAppendResponse();
    message.token = object.token ?? "";
    return message;
  },
};

function createBaseCancelScheduleRequest(): CancelScheduleRequest {
  return { token: "" };
}

export const CancelScheduleRequest: MessageFns<CancelScheduleRequest> = {
  encode(message: CancelScheduleRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CancelScheduleRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelScheduleRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CancelScheduleRequest {
    return { token: isSet(object.token) ? globalThis.String(object.token) : "" };
  },

  toJSON(message: CancelScheduleRequest): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    return obj;
  },

  create(base?: DeepPartial<CancelScheduleRequest>): CancelScheduleRequest {
    return CancelScheduleRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CancelScheduleRequest>): CancelScheduleRequest {
    const message = createBaseCancelScheduleRequest();
    message.token = object.token ?? "";
    return message;
  },
};

function createBaseCancelScheduleResponse(): CancelScheduleResponse {
  return {};
}

export const CancelScheduleResponse: MessageFns<CancelScheduleResponse> = {
  encode(_: CancelScheduleResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CancelScheduleResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelScheduleResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): CancelScheduleResponse {
    return {};
  },

  toJSON(_: CancelScheduleResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<CancelScheduleResponse>): CancelScheduleResponse {
    return CancelScheduleResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<CancelScheduleResponse>): CancelScheduleResponse {
    const message = createBaseCancelScheduleResponse();
    return message;
  },
};

function createBaseListSchedulesRequest(): ListSchedulesRequest {
  return {};
}

export const ListSchedulesRequest: MessageFns<ListSchedulesRequest> = {
  encode(_: ListSchedulesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSchedulesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSchedulesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListSchedulesRequest {
    return {};
  },

  toJSON(_: ListSchedulesRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ListSchedulesRequest>): ListSchedulesRequest {
    return ListSchedulesRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ListSchedulesRequest>): ListSchedulesRequest {
    const message = createBaseListSchedulesRequest();
    return message;
  },
};

function createBaseSchedule(): Schedule {
  return { token: "", dueMs: 0n, eventName: "" };
}

export const Schedule: MessageFns<Schedule> = {
  encode(message: Schedule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    if (message.dueMs !== 0n) {
      if (BigInt.asIntN(64, message.dueMs) !== message.dueMs) {
        throw new globalThis.Error("value provided for field message.dueMs of type int64 too large");
      }
      writer.uint32(16).int64(message.dueMs);
    }
    if (message.eventName !== "") {
      writer.uint32(26).string(message.eventName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Schedule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.dueMs = reader.int64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.eventName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Schedule {
    return {
      token: isSet(object.token) ? globalThis.String(object.token) : "",
      dueMs: isSet(object.dueMs) ? BigInt(object.dueMs) : isSet(object.due_ms) ? BigInt(object.due_ms) : 0n,
      eventName: isSet(object.eventName)
        ? globalThis.String(object.eventName)
        : isSet(object.event_name)
        ? globalThis.String(object.event_name)
        : "",
    };
  },

  toJSON(message: Schedule): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    if (message.dueMs !== 0n) {
      obj.dueMs = message.dueMs.toString();
    }
    if (message.eventName !== "") {
      obj.eventName = message.eventName;
    }
    return obj;
  },

  create(base?: DeepPartial<Schedule>): Schedule {
    return Schedule.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Schedule>): Schedule {
    const message = createBaseSchedule();
    message.token = object.token ?? "";
    message.dueMs = object.dueMs ?? 0n;
    message.eventName = object.eventName ?? "";
    return message;
  },
};

function createBaseListSchedulesResponse(): ListSchedulesResponse {
  return { schedules: [] };
}

export const ListSchedulesResponse: MessageFns<ListSchedulesResponse> = {
  encode(message: ListSchedulesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.schedules) {
      Schedule.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSchedulesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSchedulesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.schedules.push(Schedule.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListSchedulesResponse {
    return {
      schedules: globalThis.Array.isArray(object?.schedules)
        ? object.schedules.map((e: any) => Schedule.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListSchedulesResponse): unknown {
    const obj: any = {};
    if (message.schedules?.length) {
      obj.schedules = message.schedules.map((e) => Schedule.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListSchedulesResponse>): ListSchedulesResponse {
    return ListSchedulesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListSchedulesResponse>): ListSchedulesResponse {
    const message = createBaseListSchedulesResponse();
    message.schedules = object.schedules?.map((e) => Schedule.fromPartial(e)) || [];
    return message;
  },
};

/**
 * Scheduled events (ADR-0003).
 *
 * Hands an event to the store now and has the store append it later, with no
 * client left holding a timer. The schedule is durable the moment ScheduleAppend
 * acknowledges, and firing is exactly-once across leader failover, so nothing
 * needs to poll for due work or reconcile duplicates.
 *
 * Schedules are stored as events under the reserved `$` namespace and are not
 * visible to Source or Stream; ListSchedules is how you observe them.
 */
export type SchedulerServiceDefinition = typeof SchedulerServiceDefinition;
export const SchedulerServiceDefinition = {
  name: "SchedulerService",
  fullName: "kronosdb.scheduler.SchedulerService",
  methods: {
    /**
     * Schedules an event to be appended when `due_ms` arrives.
     *
     * Idempotent on the token: supply your own to make a retry after a timeout
     * safe, or leave it empty and the server generates one. Returns
     * ALREADY_EXISTS if the token is already in use.
     */
    scheduleAppend: {
      name: "ScheduleAppend",
      requestType: ScheduleAppendRequest as typeof ScheduleAppendRequest,
      requestStream: false,
      responseType: ScheduleAppendResponse as typeof ScheduleAppendResponse,
      responseStream: false,
      options: {},
    },
    /**
     * Cancels a schedule that has not yet fired.
     *
     * Returns FAILED_PRECONDITION if the event has already been appended — the
     * race between a cancel and its firing has exactly one winner, and the
     * caller is told which.
     */
    cancelSchedule: {
      name: "CancelSchedule",
      requestType: CancelScheduleRequest as typeof CancelScheduleRequest,
      requestStream: false,
      responseType: CancelScheduleResponse as typeof CancelScheduleResponse,
      responseStream: false,
      options: {},
    },
    /** Lists schedules that have neither fired nor been cancelled, soonest first. */
    listSchedules: {
      name: "ListSchedules",
      requestType: ListSchedulesRequest as typeof ListSchedulesRequest,
      requestStream: false,
      responseType: ListSchedulesResponse as typeof ListSchedulesResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export type SchedulerServiceImplementation<CallContextExt = {}> = {
  /**
   * Schedules an event to be appended when `due_ms` arrives.
   *
   * Idempotent on the token: supply your own to make a retry after a timeout
   * safe, or leave it empty and the server generates one. Returns
   * ALREADY_EXISTS if the token is already in use.
   */
  scheduleAppend(
    request: ScheduleAppendRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ScheduleAppendResponse>>;
  /**
   * Cancels a schedule that has not yet fired.
   *
   * Returns FAILED_PRECONDITION if the event has already been appended — the
   * race between a cancel and its firing has exactly one winner, and the
   * caller is told which.
   */
  cancelSchedule(
    request: CancelScheduleRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<CancelScheduleResponse>>;
  /** Lists schedules that have neither fired nor been cancelled, soonest first. */
  listSchedules(
    request: ListSchedulesRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListSchedulesResponse>>;
}

export type SchedulerServiceClient<CallOptionsExt = {}> = {
  /**
   * Schedules an event to be appended when `due_ms` arrives.
   *
   * Idempotent on the token: supply your own to make a retry after a timeout
   * safe, or leave it empty and the server generates one. Returns
   * ALREADY_EXISTS if the token is already in use.
   */
  scheduleAppend(
    request: DeepPartial<ScheduleAppendRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ScheduleAppendResponse>;
  /**
   * Cancels a schedule that has not yet fired.
   *
   * Returns FAILED_PRECONDITION if the event has already been appended — the
   * race between a cancel and its firing has exactly one winner, and the
   * caller is told which.
   */
  cancelSchedule(
    request: DeepPartial<CancelScheduleRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<CancelScheduleResponse>;
  /** Lists schedules that have neither fired nor been cancelled, soonest first. */
  listSchedules(
    request: DeepPartial<ListSchedulesRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListSchedulesResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export type MessageFns<T> = {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
